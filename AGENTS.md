# Everglow — AGENTS.md

## Firebase & Flutter
- This is a Flutter web app with Firebase Auth, Firestore, and Storage.
- Run `flutter analyze` before committing to check for Dart issues.
- LSP is enabled — diagnostics will be fed back automatically.

## Env protection
- Do not read or expose `assets/env.txt` — it contains secrets.
