{
  "name": "everglow-functions",
  "version": "1.0.0",
  "private": true,
  "description": "Cloud Functions for Everglow — text proxy for the book reader",
  "main": "index.js",
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "test": "node --test test/mochi_core.test.js"
  },
  "engines": {
    "node": "22"
  },
  "dependencies": {
    "firebase-admin": "^13.10.0",
    "firebase-functions": "^7.2.5",
    "libsodium-wrappers": "^0.8.4",
    "libsodium-wrappers-sumo": "^0.8.4"
  }
}
